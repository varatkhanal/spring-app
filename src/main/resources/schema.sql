DROP TABLE enum_zone IF EXISTS;
DROP TABLE enum_districts IF EXISTS;

CREATE TABLE IF NOT EXISTS enum_zone (
  zones VARCHAR(30)
);


create table IF NOT EXISTS enum_districts(
 districts  VARCHAR(30)
);

